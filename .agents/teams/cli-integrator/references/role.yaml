name: cli-integrator
description: "CLI async ingest integration role for agent-team"
system_prompt_file: system.md
scope:
  in_scope:
    - "Go CLI commands"
    - "non-blocking retry and logging"
  out_of_scope:
    - "web UI implementation"
    - "database schema ownership"
constraints:
  single_role_focus: true
skills: []
