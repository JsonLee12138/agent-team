name: backend-ingest
description: "Backend ingest and database schema role"
system_prompt_file: system.md
scope:
  in_scope:
    - "ingest API"
    - "Postgres schema and indexes"
  out_of_scope:
    - "frontend pages"
    - "CLI command changes"
constraints:
  single_role_focus: true
skills: []
