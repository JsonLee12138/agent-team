interface:
  display_name: "Workflow"
  short_description: "Governance workflow plan lifecycle"
  default_prompt: "Use $workflow to manage governance workflow plans with `agent-team workflow plan generate|approve|activate|close`."
